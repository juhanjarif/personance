{
  "name": "personance",
  "version": "1.0.0",
  "description": "Personance Full Stack Application",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
